/**
 *人员信息维护 
 **/
(function(){
	
	var column=Tx.auto.TxGrid.getColumns({
		sqlid:"",
	});
	var cmp = Ext.Panel.create({
		region : 'center',
		layout : "border",
		items:[]
	});
	return cmp;
})();